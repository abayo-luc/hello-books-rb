require 'rails_helper'

RSpec.describe V1::UsersController, type: :controller do
  describe 'POST #create' do
    # subject { post :create, params: { email: 'luc@gmail.com', password: 'password' }}
    # # it { should have_http_status(:created) }
    # it {should permit(:email, :password, :first_name, :last_name).for(:create, verb: :post)}
  end
end
